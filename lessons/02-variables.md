# 第2课：变量与数据类型

## 变量声明

```rust
fn main() {
    let x = 5;
    println!("x = {}", x);
}
```

Rust 用 `let` 声明变量。

## 默认不可变！

```rust
fn main() {
    let x = 5;
    x = 6;  // ❌ 编译错误！
}
```

Rust 变量**默认不可变**（immutable），这是 Rust 安全性的核心设计。

要让变量可变，加 `mut`：

```rust
fn main() {
    let mut x = 5;
    println!("x = {}", x);
    x = 6;  // ✅ OK
    println!("x = {}", x);
}
```

## 什么时候用 `mut`？

- 计数器、累加器
- 循环里要修改的状态
- 构建一个东西（比如拼字符串、往 Vec 里 push）

## 基本数据类型

### 整数
| 类型 | 大小 | 说明 |
|-----|------|------|
| `i8` / `u8` | 8-bit | 有符号 / 无符号 |
| `i16` / `u16` | 16-bit | |
| `i32` / `u32` | 32-bit | i32 是默认整数类型 |
| `i64` / `u64` | 64-bit | 大整数 |
| `usize` | 平台相关 | 用于索引 |

### 浮点数
```rust
let f1 = 3.14;       // f64（默认）
let f2: f32 = 2.5;   // f32
```

### 布尔
```rust
let t = true;
let f: bool = false;
```

### 字符
```rust
let c = 'A';
let emoji = '🦀';  // 支持 Unicode
```

## 类型推断 vs 显式标注

```rust
let guess = 42;          // 编译器推断为 i32
let guess: u32 = 42;     // 显式指定 u32
```

## 常量

```rust
const MAX_POINTS: u32 = 100_000;
```

- 用 `const` 声明
- **必须**标注类型
- 命名惯例：全大写 + 下划线

## Shadowing（变量遮蔽）

```rust
fn main() {
    let x = 5;
    let x = x + 1;      // 新的 x 遮蔽旧的
    let x = x * 2;
    println!("x = {}", x);  // 输出 12
}
```

Shadowing 允许复用变量名，甚至改变类型：

```rust
let spaces = "   ";         // &str
let spaces = spaces.len();  // usize，合法！
```

## 实战规则：选哪个类型？

| 场景 | 用什么 |
|-----|--------|
| 数组索引、`.len()` | `usize`（必须的） |
| 数据库 ID | `i64` 或 `u64` |
| 一般计算 | `i32`（默认就行） |
| 年龄、数量等肯定非负 | `u32` |
| 和 C/外部 API 交互 | 看文档要求 |

---

**下节课**：所有权 Ownership
